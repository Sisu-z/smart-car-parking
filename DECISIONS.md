# 工程决策日志

## DEC-016——GitHub 共享真源与公开边界（2026-09-22）

- 状态：`ACCEPTED`，用户授权公开发布并维护；仓库为 `Sisu-z/smart-car-parking`。
- 问题：不同对话/Agent 不共享隐式上下文，工作区此前没有版本管理。
- 选择：GitHub main 为已集成基线，任务分支为在途工作；唯一总体方案，事实/接口/交接分工明确；每次实质变更同步文档、证据、提交和分支，复审后集成。
- 否决：多个“最新版”；只在聊天里改需求；多人直接写 main；自动公开厂家包或私人日志。
- 影响：README、AGENTS、模板、交接。厂商原件/学习 HAL/原始采集数据不公开；第三方保留版本与许可证；公开不等于统一授予开源许可。
- 允许重议：可见性、许可、协作规模由用户调整；不自动创建定时维护，不承诺 Agent 未运行时持续更新。

## DEC-015——先实测视野，再定相机组合（2026-09-22）

- 状态：`ACCEPTED` 的选型流程，具体硬件仍 `PROVISIONAL`。
- 选择：现有 USB 相机先做样本/标定/视野验证；云台、镜面、双/四相机都是候选。增补优先 ≤1000 元，收益显著再讨论 ≤2000 元。
- 原因：相机数量不等于可观测性或总成本；先检查最可能返工的盲区。
- 否决：默认四摄；称镜面/云台已证明最省钱；强化学习作为前置条件。
- 影响：M3 与采购；用户报告 X5 8GB、标称 90° USB 相机、无 IMU/雷达，电气/性能尚未核验。
- 允许重议：按盲区/误差/延迟/预算实测调整；采购另获授权。

## DEC-014——第一阶段七个里程碑与跨场地验收（2026-09-22）

- 状态：`ACCEPTED` 当前拆分；数值指标待测量后确认。
- 选择：M0 安全/边界；M1 单电机闭环；M2 底盘/通信；M3 普通线感知（可并行）；M4 近距离倒库/出库；M5 随机找位；M6 跨场地/故障验收。
- 原因：中间关卡可调试，但不是最终交付；提前验证视觉可观测性。
- 否决：固定起点成功即完工；事后放宽指标；安全拒绝计为停车成功。
- 影响：总体方案、测试、待办；C API v0.1 保持，未来感知/任务接口仍为未实现草案。
- 允许重议：细分可依据证据调整；用户核心目标变更须确认。

## DEC-013——普通线、随机找位、倒库/出库为第一阶段目标（2026-09-22）

- 状态：`ACCEPTED`，用户明确要求，替代 DEC-010。
- 选择：安全可行区域不同位置/朝向下自主找普通车位、在线接近、末段倒车入库、停稳，按指令出库；展示场地可不同于开发场地。
- 原因：目标是可跨场地的自主任务，不是记住房间坐标或定时动作。
- 否决：运行时 AprilTag/二维码定位；人工提供初始位姿/车位坐标；固定动作回放。固定起点仅作中间测试。
- 影响：总体方案、感知/任务接口、验收。旧标记工具仅隔离实验；允许棋盘离线标定；完整 SLAM 不自动成为前置。
- 允许重议：证据可调整工作范围；未经用户确认不得降成标记或固定发车区方案。

## DEC-012——开源优先与离线交付范围（2026-09-22）

- 状态：`ACCEPTED`，依据本轮用户明确授权。
- 选择：复用现有 C 模块、现有 STM32 HAL 和 PythonRobotics 算法；只新增项目所需的安全门控、适配、测试和结果查看。保留来源、许可证和固定版本。
- 调整：学习性重写不再是交付前提；新建独立单电机候选工程，不改 `test01`，不必为了单电机试验先迁移舵机。
- 实验范围：二维阿克曼模型仅为 `[EXPERIMENTAL]`，不认定实物底盘类型；不创建完整 ROS2/SLAM、机械臂或最终硬件方案。
- 接口复审：保留 v0.1 命令和遥测；新增独立 bench 安全层。非法输入停机、超时锁存、重新使能必须显式解除故障；正反换向要求先禁用并观察编码器静止。正式 RDK 会话/序列号/校验协议尚未实现，不宣称协议已定型。
- 成本边界：同一问题最多三次有依据的修复尝试；仍失败则记录原始失败、绕过方式和影响，继续独立工作，不削弱验收来制造通过。
- 允许重议：实物参数确认、上板数据或更省成本的成熟实现出现时可以。

状态：`ACCEPTED` 已接受、`PROVISIONAL` 暂定、`SUPERSEDED` 已被替代。“允许重议”说明未来证据是否可以改变决定。

## DEC-001——划分高层与底层职责

- 日期：2026-09-21
- 状态：`ACCEPTED`
- 问题：实时控制与自主能力分别放在哪里？
- 选择：STM32 负责 motor/encoder/servo/safety/speed control；RDK X5 负责 perception、parking logic 和高层命令。
- 原因：执行器安全和确定性控制不依赖 Linux/感知延迟。
- 否决：RDK 直接控制 PWM；STM32 承担完整视觉和规划。
- 影响：全部固件和未来通信接口。
- 允许重议：可以调整边界，但 RDK 仍不得直接操作 HAL/PWM。

## DEC-002——自动泊车是第一个高级交付目标

- 日期：2026-09-21
- 状态：`ACCEPTED`
- 选择：先完成稳定底盘，再完成受控环境中的自动泊车/出库；机械臂为 Optional。
- 原因：能验证完整自主链路，又不强迫项目一开始构建通用机器人全栈。
- 否决：把普通循迹/避障当最终定位；底盘与机械臂同时开发。
- 影响：路线图和验收测试。
- 允许重议：若硬件或时间证据证明泊车不可行，可以重议，但必须记录。

## DEC-003——推迟高返工范围

- 日期：2026-09-21
- 状态：`ACCEPTED`
- 选择：当前不做最终 PCB/CAD、RTOS、完整 ROS2/SLAM、最终传感器套装或完整机械臂。
- 原因：需求和底盘细节未冻结，这些工作不能解除未来 1～3 个阶段的阻塞。
- 否决：在电机反馈尚未跑通前搭建企业级框架。
- 影响：任务优先级。
- 允许重议：V0 底盘获得实测证据、并出现具体需求后可以。

## DEC-004——厂商证据决定硬件事实边界

- 日期：2026-09-21
- 状态：`ACCEPTED`
- 选择：每条硬件事实必须带来源位置；缺失值保持 `[UNKNOWN]`；相似型号不能填补缺口。
- 原因：MG513/D153 的型号和版本差异已经证明外推有风险。
- 否决：把网络常见值或同系列参数当项目事实。
- 影响：全部文档、配置与安全决策。
- 允许重议：原则不重议；只有更可靠证据可以更新具体值。

## DEC-005——原型阶段暂用 D153/TB6612，但等待电流证据

- 日期：2026-09-21
- 状态：`PROVISIONAL`
- 选择：使用现有 D153C/D153B TB6612 做低占空比、有电流观察的 bring-up；暂不声称它最终适配 MG513XP28_12V。
- 原因：模块已具备且资料较全，但电机精确电流和板版本仍未知。
- 否决：立即设计 PCB；用 3.2 A 脉冲值证明驱动充分。
- 影响：B01～B12 测试和供电安全。
- 允许重议：取得电机电流资料或实测后立即重议。

## DEC-006——统一公开单位与正方向

- 日期：2026-09-21
- 状态：`ACCEPTED`
- 选择：Motor duty 为 `[-1,+1]`；V0 速度环使用输出轴 RPM；vehicle 层使用 m/s 和 rad/s；坐标系为 `+x` 前、`+y` 左、`+z` 上；正转向/yaw 为左转/逆时针。
- 原因：避免不同 Agent 混用 timer count、任意 PWM 范围和相反坐标系。
- 否决：把 `0..7199`、`0..1000` 或无符号 PWM 暴露为模块接口。
- 影响：固件、协议、遥测、泊车。
- 允许重议：只有升级接口版本时允许。

## DEC-007——先 PI，后决定是否需要 PID

- 日期：2026-09-21
- 状态：`ACCEPTED`
- 选择：先 P，再 PI 与 anti-windup；只有实测证明有稳定需求时才启用 D。
- 原因：厂家所谓“PID”实际是基于原始窗口计数的 PI，D 会放大速度估计噪声。
- 否决：复制厂家增益；因为文件名叫 PID 就加入 D。
- 影响：控制器模块与调参手册。
- 允许重议：获得 PI 实测曲线后可以。

## DEC-008——保留 `test01`，通过新配置完成集成

- 日期：2026-09-21
- 状态：`ACCEPTED`
- 选择：构建资产期间不修改当前已跑通的 servo/UART 工程；新模块先独立 host test，通过 CubeMX 引脚/定时器复审后再集成。
- 原因：当前 TIM3 以 50 Hz 驱动舵机，而 D153 参考需要同一个 TIM3 的 CH3/CH4 输出 10 kHz；直接生成会破坏已有基线。
- 否决：立即把 motor 代码粘入当前 `main.c`。
- 影响：固件工作流和下一步动作。
- 允许重议：保护已有基线的原则不变；集成必须通过副本或分支进行。

## DEC-009——暂定集成定时器分配

- 日期：2026-09-21
- 状态：`PROVISIONAL`
- 选择：把 Servo 从 PA6/TIM3_CH1 移到 PA8/TIM1_CH1（50 Hz）；TIM3 CH3/PB0 与 CH4/PB1 专用于 10 kHz Motor PWM；TIM2 PA0/PA1 与 TIM4 PB6/PB7 用于编码器；保留 USART1 PA9/PA10。初期使用非阻塞 10 ms 调度，必要时再升级专用周期中断。
- 原因：同一定时器只有一个基频；当前 50 Hz 与目标 10 kHz 不能在 TIM3 同时实现。迁移还会释放 PA6 给 D153 电压 ADC。
- 否决：同一 TIM3 同时承担两种频率；未测量资源需求就启用第二块 STM32；过早引入 RTOS。
- 影响：CubeMX 集成和接线。
- 允许重议：可以。冻结前必须结合真实舵机与底盘复审。

## DEC-010——第一版泊车采用受控环境

- 日期：2026-09-21
- 状态：`PROVISIONAL`
- 当前状态：`SUPERSEDED`，2026-09-22 被 DEC-013 替代；本节原状态与理由仅作历史，不能继续执行。
- 历史选择：V1 从已知泊车区域和明确视觉参照物（如 AprilTag/已知标记）开始，配合车轮/转向反馈和有边界的泊车状态机；不把 SLAM 设为前置条件。
- 原因：用较少未知量验证完整的“感知→规划→控制”链路，并形成可复用接口与日志。
- 否决：第一阶段直接完成未知环境 SLAM；纯定时开环泊车。
- 影响：V1 路线与后续 RDK 实验。
- 允许重议：完成 RDK X5/摄像头重复性实验后可以。

## DEC-011——V0 使用文本协议，最终 RDK 传输方式暂缓冻结

- 日期：2026-09-21
- 状态：`ACCEPTED FOR V0`
- 选择：bring-up 阶段继续使用换行分隔的可读命令和 CSV 遥测；最终 RDK 链路暂不决定 UART 还是 CAN。
- 原因：与已验证的 VOFA+/CH340 工作流一致，解析和调试成本最低。
- 否决：尚未出现带宽/可靠性需求时引入复杂二进制协议。
- 影响：command、telemetry、VOFA+ 手册。
- 允许重议：RDK 集成时可以；V0 调试协议继续保留。
