# 智能移动机器人总体方案与第一阶段路线

更新：2026-09-23。状态：[DESIGN]。这是当前唯一总体方案，替代旧版“已知标记泊车优先”路线，见 DEC-013～017。以下是目标，不是完成声明。

实施增量：已新增 OpenCV 普通 U 形线框候选/固定相机投影/媒体与数据工具，规划末段倒车与独立重规划出库的离线实验。未完成实拍语义/占用验证、自主找位、入口跨越约束或实车闭环；不代表 M3/M4 整体过关。用法与边界见 offline/README.md。

## 1. 第一阶段交付什么

在约定的安全、可通行区域内，把车放到不同位置和朝向，发送停车指令。车辆自主寻找普通车位线、识别可用车位、估计相对位置、规划接近路线、调整姿态，最后以倒车方式入库并确认停稳。收到独立出库指令后，重新检查通路，驶出并停在安全位置。

开发地点与展示地点可以不同，不要求复原同一房间、同一车位坐标或同一起点。起步时允许车位不在相机视野内；寻找车位是交付内容。运行期间不使用 AprilTag、二维码等专设定位标记，不要求人工输入车辆初始位姿和车位坐标。

“随机”不等于放在不可观察、无法转身或不可达的位置也必须成功。“跨场地”不等于适配所有地面、天气和车位形式。首版工作范围拟定为：室内平整地面、可识别的普通线框车位、有足够机动空间、低速、有人监督。具体尺寸、线色/对比度、照度、起始区域/朝向、障碍物类别、速度与停止距离在 M0 登记，最终验收前冻结。条件不足应明确拒绝或退出，不能盲走。

普通胶带或油漆形成的车位线可以作为目标；不能把测试区坐标、动作时长、房间尺寸或隐藏标记当运行答案。车辆模型、标定参数、状态机和规划约束可以固定；起点、车位相对位置、接近路线和动作距离必须来自当次观测与反馈。

### 完整任务状态（设计，尚未实现）

自检/待命 → PARK → 安全观察与找位 → 确认车位、空闲与可达性 → 在线规划/接近 → 调整入库姿态 → 倒车入库 → 验证停稳 → PARKED。

PARKED → EXIT → 重新检查通路 → 规划出库 → 驶出停稳 → IDLE。

任一运动状态 → CANCEL/超时/失效/风险 → 请求停止 → 确认停止或报告停止失败。恢复连接/观测不等于自动恢复运动。

最后越过车位入口的入库段必须是倒车，途中允许前进调整。出库不是无条件倒放旧轨迹。

## 2. 已有条件和能力边界

- [PROJECT-CONTEXT] 用户确认 RDK X5 为 8GB，已有标称 90° 广角 USB 相机，目前无 IMU/雷达。标称视场不等于实测水平/垂直视场。新增预算优先不超过 1000 元，显著收益才讨论扩至 2000 元。这是用户报告，不是厂商电气事实。
- STM32F103C8T6、MG513XP28_12V/GMR、D153/TB6612、底盘、舵机和电源证据以 01_hardware_source_of_truth.md 为准。实际底盘结构仍待登记，不能因四轮和舵机就认定阿克曼。
- [TESTED-BY-TEAM] GPIO/PWM/舵机/UART/VOFA+ 有团队已跑通基线，不代表本轮重新实测。
- [IMPLEMENTED] 已有纯 C 控制/解析/安全模块、电脑端测试、默认锁定的单电机固件候选、二维规划跟踪实验、标定和日志工具；历史验证见 HANDOFF.md。
- [EXPERIMENTAL] 现有仿真用假设的阿克曼模型和模拟真值位姿；3 个固定起点泊车与 4 个故障场景不是随机找位成功率。AprilTag 工具仅保留为隔离实验，不是普通车位线识别。
- [UNVERIFIED] 实拍普通线感知、真实相机闭环、随机找位、实车闭环/出库、倒车跨越车位入口、正式 RDK 链路与跨场地端到端能力均未完成验收；离线规划末段方向约束不等于这些能力。

## 3. 总体架构与开源复用边界

| 层 | 职责 | 复用与限制 |
|---|---|---|
| STM32 | 电机/舵机、编码器、速度估算、PI、执行器门控、命令超时 | 复用现有纯 C 与 HAL；极性/CPR/增益/停止行为必须实测 |
| 车辆层 | 运动学、转向标定、里程计、车辆级运动目标 | 实物确认后选择模型，不照搬仿真参数 |
| RDK 感知 | 图像、车位线、入口/朝向/几何、空闲空间和观测质量 | 先复用成熟图像/几何算子，用实拍数据评估是否需要学习模型 |
| RDK 任务/规划 | 找位、候选选择、接近/倒车轨迹、出库、故障与恢复 | 固定版本 PythonRobotics 作几何基线，补任务约束，不视为完整自主系统 |
| 开发工作台 | VOFA+、电脑端测试、日志、回放、故障注入 | 扩充现有工具，不另造大而全调试平台 |

优先“车位相对坐标 + 有界局部里程计/局部空间表示”，完整全局 SLAM 不是默认前提。找位仍须处理已观察区域、未知空间和定位误差；若局部方法实测不足，应记录证据后升级，不声称完全不需要定位。

现有 Reeds–Shepp 只枚举几何候选，离散车身碰撞检查不是连续扫掠安全证明。必须结合实际底盘、车身/悬长、转向扫掠、可观测空闲空间和末段倒车约束；若无法绕障，再评估局部搜索规划器，不直接安装整个自动驾驶栈。

## 4. 第一阶段分成七个小阶段

M0～M6 都属于第一阶段。M4 的简化演示不是整个第一阶段完成。

### M0：定边界、硬件身份和验收方法

**产出**：硬件配置、电源/断能方案、底盘结构与尺寸清单、相机视野样本、测试矩阵草案。登记的是安全工作范围，不是提供给车辆的车位答案。

**工作**：确认驱动板版本/电压、电机电流证据、编码器供电；确认转向与驱动结构，量车身/轴距或轮距/车轮；拍摄接近到入库末端的相机视野。提前划分开发场景和独立验收场景。

**通过**：安全未知项已足以支持限流、无负载单电机测试；否则保持硬件门控。工作范围有草案，数值可在 M1～M3 测量后冻结，不能用低 duty 代替限流。

**分工**：用户提供实物、照片、测量和安全断电；Agent 审查差异/资源、安全及验收记录。无需等电机完成才开始静态视觉采样。

### M1：单电机 → 编码器 → RPM → P/PI

**顺序**：先无电机测 PWM/方向，再无负载低风险启停；手转编码器验证方向/回绕；多圈标定每输出轴计数；测采样周期；RPM/VOFA+；最后 P→PI、限幅与 anti-windup。

**产出**：实测配置、方向/计数/速度曲线、启停和故障记录。复用独立单电机候选，原 test01 不变。

**通过**：反馈符号一致、没有伪 RPM；断连/非法命令/超时不继续旧目标；停止行为和滑行距离有测量；PI 在限定速度/负载内可重复且退出饱和后可恢复。阈值在实验前登记，不复制仿真增益。

**失败处理**：电流异常先停；符号/比例不明退回 raw count，不靠调 PI 掩盖。

### M2：形成可控底盘与可靠命令链路

**前置**：M1 + 实际底盘结构确认。独立单电机不需先迁移舵机；联合工程才解决 TIM3 冲突并回归舵机/UART。

**产出**：所需多路驱动/编码器/转向集成；转向零点、限位、回差、最小转弯半径、低速区和停止包络；车辆 API 与局部里程计误差；带会话/序列号/校验与新鲜度规则的 RDK 原型链路，传输方式由实测决定。

**通过**：从 RDK 下发有限运动目标可执行、可观测；前进/后退/左右转/停机可重复；断线、延迟、重复/乱序、跨重启旧命令不能触发意外恢复；无效里程计明确报告。软件停止请求与真实停稳分别验证。

**分工**：用户负责接线、上板、架空/低速实测；Agent 提供候选固件、通信测试和日志分析，不代替物理测量。

### M3：普通车位线感知与最小相机配置

**与 M1/M2 并行**：先静态照片/录像，不依赖车辆自主运动。棋盘可以用于离线标定，但不能变成运行时定位标记。

**产出**：普通线框、地砖/反光干扰、部分遮挡、无车位、不同视角数据；开发/验证集分离；入口/边界/朝向/尺度/相对位姿/质量输出；可见范围与帧延迟测量。

**方法**：先复用图像处理、直线和几何筛选，实测不够再比较学习检测器。地面米制投影需要真实内外参与平面假设；看见像素线不等于获得真实位姿。车位线可见不等于车位空闲，障碍/未知空间单独表达。

**通过**：未参与调参的样本上能区分车位/非车位，入口方向不反；位姿误差和可用距离有记录；低质量、遮挡、视野外不编造有效位置。测评前冻结阈值。

**硬件决策**：先测已有固定相机。云台、镜面、前后双相机、四相机均是候选而非采购结论。云台需逐帧姿态/时间/外参，镜面需验证反射几何/畸变/遮挡；不能只按摄像头数量判断工程成本。若盲区/延迟/误差阻塞任务，再按预算比较增补。软件不能凭空补出没有观测的全向安全能力。

### M4：先做“看得到车位”的闭环倒库与出库

**前置**：M2 + M3；近距离可观测、可通行、可达起点，只作为内部集成条件。

**工作**：每次从观测计算目标与入库准备姿态；在线规划、反馈跟踪、车身/扫掠检查、停稳换向，末段倒车越过入口。按位置/航向/车身边界/速度确认停稳；独立 EXIT 后重新检查通路再出库。

**通过**：多个近距离起点重复通过，不只拍一次成功视频；感知丢失/跟踪误差/阻挡/CANCEL 触发停止并解释；不自动盲目恢复。误差、耗时、换向次数、间隙和失败原因留日志。

**限制**：这一关只证明局部泊车，不满足“随机摆放后自主找位”。

### M5：随机位置/朝向自主找位，再泊入

**前置**：M4 + 经验证的观察/搜索与空间安全策略。测试必须包括起步看不到车位。

**工作**：先观察再作有界低速搜索，维护已观察范围，避免循环搜索；持续确认车位身份/入口/空闲/可达性；在线到达入库准备区并接 M4。重观察/重规划有次数和时间上限。

**通过**：随机起点仅给测试记录/评估器，不给运行算法当答案；车位坐标由观测获取。可达正常场景完成全流程；无车位、占用、不可达、观测失效等及时说明并停止。未知区域不得默认可通行。

**风险**：侧后方盲区、转向几何、短时漂移与空闲空间判断。实测不足时优先改观察策略或有限增补模块，不悄悄缩成固定发车区。

### M6：跨场地与故障验收，证明不是背动作

**前置**：M5；工作范围、位置/航向容差、时间上限、停车速度、安全间隙和试验数量在最终测试前冻结。

**验收结构 [DESIGN]**：至少一个开发场景和一个未参与调参的展示类场景；改变车位位置/方向、起始位置/朝向、照明与干扰，在独立场景用同一版本运行。保留试验清单/随机种子，起点可做物理可达性/安全筛选，不能挑选已成功的点冒充随机。

**分别报告**：正常有效场景泊车成功数/总数、出库成功数/总数、完整任务成功数/总数、失败数、拒绝数、范围外数量及安全事件。拒绝不是泊车成功。保留终点误差、越线、停稳、耗时、接管、碰撞/越界、故障响应与视频/日志索引。

**通过**：满足预登记指标；正常全流程和安全负例都验收；无运行标记、手输初始/车位坐标或场地专用定时脚本。更换相机安装必须重标外参，但仅换房间不能靠重新记住车位坐标过关。

现在明确验收结构，不虚构厘米级/成功率承诺。数值由 M1～M3 数据与车位尺寸提出、确认、冻结；不能验收失败后放宽制造通过。

## 5. 执行依赖与提速办法

M0 → M1 → M2；M3 静态感知/视野验证可并行。M2 + M3 → M4 → M5 → M6。

当前最值得做的是用户推进安全台架，同时 Agent 用实拍数据推进普通线和视野验证，补通信故障测试、规划末段约束/任务状态机。没有真实图片时可先做数据格式、测试和回放，不宣布识别效果。

电脑端可先验状态转换、超时/取消/失败、几何规划、随机场景、边界和接口，再做录像回放和实车闭环。复用现有测试入口/回放页，不先造大型场景引擎。仿真只消除部分逻辑风险，不等于实物已经安全可控。

## 6. 关键风险与延后范围

| 风险 | 动作 |
|---|---|
| 电流、板版本、电源树未知 | 保持门控；不带载、不故意堵转 |
| Servo/Motor TIM3 冲突 | 单电机独立；联合工程复审 DEC-009 |
| 假 RPM/错误方向/时基 | raw count、多圈与周期验证后才 PI |
| coast 被误当可靠刹车 | 实测停止包络，独立断能与人工监督 |
| 像素检测冒充米制定位 | 标定、地面假设、独立测量、留出样本 |
| 盲区/未知障碍 | UNKNOWN 不当 FREE，限制动作或增补观测 |
| 固定仿真冒充跨场地 | M4 仅内部关卡，M5/M6 单独验收 |
| 开源代码不匹配或许可不明 | 固定版本、保留许可、适配后测试 |

暂不做最终 PCB/CAD、完整机械臂、通用具身模型、强化学习实车探索、完整 ROS2/SLAM、量产自动驾驶全栈。强化学习以后只能作为有明确基线/收益的隔离实验，不能代替当前可测的感知控制。

小问题最多三次有依据的尝试；仍失败则记录证据/影响/绕过办法，转做独立任务。安全门槛不能以“暂时跳过”变成冒险运行。

## 7. 【自主调整说明】

- 旧路线把标记泊车当 V1、普通线放到后续，不满足用户要求；现在普通线、随机找位、倒车入库/出库、跨场地全部纳入第一阶段，DEC-010 被替代。
- 原路线偏串行固件；相机可观测性也可能决定方案。M3 与台架并行，避免底盘完成后才发现看不见入口或买错模块。
- 保留现有开源算法、标定数学和回归资产；AprilTag/固定起点实验明确隔离，不能冒充主线完成。
- 不新增第二套总方案；本文件统一目标和里程碑，其他文档只承载事实/接口/状态/证据。GitHub 作为共享版本库，机器本地路径不作为协作前提。
