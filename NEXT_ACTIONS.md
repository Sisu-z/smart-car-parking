# 下一步行动

更新：2026-09-23。按 [总体方案](00_project_master_plan.md) M0～M6 执行；以下是待办，不代表已完成。可复用的新软件见 [offline/README.md](offline/README.md)，先跑现有工具，不再重复写线段检测/地面投影/媒体批处理。

## 现在并行两条线

### 用户：M0/M1 实物信息与安全台架

1. 拍驱动板正反面/版本、电机/编码器标签、底盘转向/驱动结构、舵机、电池/稳压模块，填写 templates/HARDWARE_CONFIG_TEMPLATE.md。
2. 确认电源树、独立断能方式、电流观察和精确 XP28 电流证据；禁止同系列外推或故意堵转。
3. 断电核对 A/B、E1/E2 与物理轮；电机断开时测电源轨/PWM/方向，板版本和供电不清楚不通电试错。
4. 按 10_bringup_checklist.md 与 03_motor_encoder_pid_bringup.md 验证独立单电机候选；默认硬件门控关闭，安全审核后才按步骤修改配置。
5. 开环 → 手转 raw count → 方向/回绕 → 多圈计数 → 实测 dt → RPM/VOFA+ → P/PI；每一步留记录再继续。

独立单电机没有舵机，不需先迁移 TIM3。M2 联合工程才在保护副本/分支验证 DEC-009、回归舵机/UART，原 test01 不变。

### 用户与 Agent 配合：M3 普通车位线与视野

1. 已有 USB 相机采普通车位线、地砖/反光、不同距离/角度、无车位照片/视频；先静态采样，不让未验收车辆自主移动；隐私图像/原视频默认本地保存。
2. 记录型号、分辨率、安装高度/俯仰和视场来源；用真实棋盘图片和实测格尺寸验证 tools/calibrate_camera.py。棋盘仅离线标定，不用于泊车导航。
3. 实现识别前划分开发与独立验证场景；核对接近/倒库末端/出库视野，记录盲区。
4. 有实拍样本后先运行 `offline.slot_cli` 现有普通 U 形候选/可选米制投影，审查成功和失败图；根据真实数据决定下一步，不宣称几何候选就是车位或空位。现有 offline.vision 仍是隔离 AprilTag 实验。
5. 实测不足再比较云台、镜面、前后双相机等总成本；不默认购买四相机或雷达。

## Agent 可继续做的后续任务

- 会话/序列号/校验/超时测试，不改变 v0.1 台架命令。
- 已有规划末段倒车、独立重规划出库和随机几何测试；接下来补车位入口跨越验证、完整任务状态机/EXIT/CANCEL、搜索与故障分支。未来感知闭环不得输入模拟真值冒充自主定位。
- 已有媒体 manifest 与标定摘要、开发/验证泄漏检查；补真实标注和精度评估，不把合成数据或去重通过当泛化证据。
- 真实尺寸/误差到位后提出数值验收指标，最终测试前冻结，不以仿真精度承诺实车表现。

## 后续顺序

M1 → M2 底盘/通信；M2 + M3 → M4 近距离倒库/出库；M4 → M5 随机找位；最后 M6 独立场地验收。M4 只是中间一关。

## 每次结束

记录提交、变更、测试范围、未验证和阻塞，更新 HANDOFF.md；事实/决策/接口/问题状态变化时同步对应真源。按 AGENTS.md 审查后上传任务分支，集成者合入主线。高风险阻塞不能跳过；一般难题最多三次有依据尝试后记录并转做独立任务。
