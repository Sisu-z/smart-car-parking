# Agent 任务模板

只复制实际需要的部分，保持任务边界清晰。

```text
【任务背景】
为什么现在需要做这项工作；当前阶段和已验证状态是什么。

【必读文件】
- START_HERE.md
- PROJECT_CONTEXT.md
- <本任务相关文件>

【目标】
一个可以观察和验收的结果。

【版本与所有权】
仓库/基础提交/任务分支/工作目录；允许修改的模块和文件；依赖的任务；Reviewer/Integrator（未指定则注明待安排）。

【输入】
准确的文件、硬件版本、日志、测量值和软件版本。

【期望输出】
准确的文件、补丁、测试或审查报告。

【不能修改】
原始资料、生成代码、已接受接口、无关模块等。

【硬件事实来源】
文件名 + 页码/章节/源码位置。缺失值保持 UNKNOWN。

【接口约束】
INTERFACE_CONTRACTS.md 和 CONVENTIONS.md 中的相关条目。

【完成标准】
需要哪些构建、测试和实机证据；哪些内容仍未验证。
使用总体方案的 M0～M6；不能用标记检测或固定起点结果替代随机普通线泊车。写清数值指标及冻结时点。

【需要更新的共享文档】
HANDOFF.md；若状态确实改变，再更新 OPEN_QUESTIONS.md、DECISIONS.md、NEXT_ACTIONS.md。
```

## Agent 完成任务后必须报告

1. 做了什么。
2. 修改了哪些文件。
3. 做了哪些假设。
4. 验证了什么，以及如何验证。
5. 哪些内容仍未验证。
6. 新发现的问题或风险。
7. 是否建议重新讨论既有决策。
8. 下一位 Agent 必须知道什么。
9. 本地提交、远端分支/PR 与同步结果；失败/跳过不能算通过；不能把“已提交”写成“已合入 main”。

状态标记遵循 `CONVENTIONS.md`。“生成了代码”只能说明 `[IMPLEMENTED]`；没有明确测试证据时不得标为 `[TESTED]`。
