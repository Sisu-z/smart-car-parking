# 多 Agent 分工与审查流程

目标是：在接口稳定时并行处理证据和实现，而不是追求最多并发。

## 推荐角色

| 角色 | 当前边界 | 可以并行的工作 | 必须等待/审查门槛 |
|---|---|---|---|
| Agent A——硬件与安全 | D153 版本、电机资料、电源树、接线、电流/温升证据 | 主机端代码、泊车研究 | 带载测试前必须复审电源和电流结论 |
| Agent B——STM32 Motor/Encoder | CubeMX 分配、HAL 适配、B01～B07 固件 | 主机 PID、泊车研究 | 电气安全依赖 A；接口变化依赖 Integrator |
| Agent C——控制算法与主机测试 | 速度估计、PI、anti-windup、仿真和测试 | A、B、D、E | counts/rev 未实测前不得宣布硬件增益有效 |
| Agent D——自动泊车路线 | 受控环境状态机、轨迹/跟踪方案、验收场景 | V0 固件工作 | 底盘 API 与定位证据完成前不得进入主固件 |
| Agent E——RDK/Vision | 普通线/入口/相对位姿、视野盲区、时间戳/质量 | M1/M2 固件，先静态实拍 | 真实数据与标定；不能把 AprilTag 验收算进主线 |
| Agent F——测试与故障分析 | 测试计划、故障注入、日志审查、通过/失败证据 | 可以审查全部 Specialist 输出 | 独立审查安全、时序与回归证据 |
| Agent G——Integrator/Reviewer | 接口兼容、决策日志、是否进入主工程 | 审查已完成工作 | 高风险项不得由同一 Agent 独立编写和批准 |

## 当前真正适合并行的任务

- 确认硬件事实、板版本和电源结构。
- 执行纯 C motor/encoder/PID/command/telemetry 主机测试。
- 在不假设未验证底盘 API 的前提下设计受控环境泊车实验。
- 收集 RDK/摄像头资料，但暂不冻结传输方式或感知框架。

## 依赖门槛（不是所有 Agent 都等到最后才工作）

```text
安全供电 + 准确板版本
→ 单电机方向/PWM
→ 编码器计数/方向
→ 每圈计数 + 实测 dt
→ RPM
→ P/PI
→ 双轮/底盘 API
→ 里程计/转向
→ RDK 命令链路 ─┐
普通线感知 M3 ──┤
                → M4 局部倒库/出库 → M5 随机找位 → M6 跨场地验收
```

任何 Agent 都不能用猜测常量替代缺失测量来跳过门槛。

## 进入主工程的流程

```text
Specialist Agent
  → 输出方案/代码 + 状态标记 + 证据
Reviewer Agent
  → 检查事实、单位、接口、故障模式和测试
Integrator
  → 接受、退回修改或继续保持实验状态
  → 更新 DECISIONS / OPEN_QUESTIONS / HANDOFF
  → 通过后才进入主工程
```

## 必须独立复审的内容

- 电源路径、电池边界、稳压负载与电机电流。
- 电机驱动接线、停止行为与换向安全。
- 编码器 CPR、解码方式和每输出轴一圈计数。
- 控制周期、速度公式、PI anti-windup 与增益单位。
- STM32↔RDK 分帧、超时与命令新鲜度。
- 泊车轨迹/跟踪、完成判定与恢复逻辑。

第一个看起来合理的答案只是候选方案，不是项目最终事实。

## GitHub 落地流程

1. 各任务报告已读 main/基础提交，明确负责路径与接口；每任务独立分支及工作目录/worktree，避免共享目录互相覆盖。
2. Specialist 提交最小改动、对应测试和 HANDOFF，推送任务分支；PR 说明范围、假设、未验证和所需审查。不把草案当可上板指令。
3. Reviewer 检查 diff、证据、数据泄露、许可证和兼容性；关键安全/控制/协议项需独立于实现者。缺审查时保留候选状态，不伪造审批。
4. Integrator 核对最新 main、解决冲突、复跑受影响测试、同步真源文档，确认后合入 main；报告合入提交/链接。没有自动强制保护配置时，此流程仍必须人为执行。
5. 后续任务从新 main 开始；不要重复抄另一对话文件或直接覆盖整个目录。网络/权限受限则明确仅本地未同步，不能静默丢失交接。

小规模阶段不要求同时开满所有角色。当前两条有效并行线是硬件安全/底盘和普通线/视野；算法/通信测试可按明确接口切小任务，不为并行额外制造协调成本。仅描述分工不代表自动创建多个 Agent。
