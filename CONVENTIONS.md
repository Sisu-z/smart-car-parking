# 统一单位、坐标系与状态标记

状态：`[DESIGN] v0.1`。以下规则适用于新模块；修改单位、方向或公开范围时，必须新增决策记录并升级接口版本。

## 单位

| 物理量 | 控制内部单位 | 人工调试单位 | 规则 |
|---|---|---|---|
| 时间 | 公式中使用秒 `float` | 时间戳使用毫秒 `uint32_t` | 变量名以 `_s` 或 `_ms` 结尾 |
| 电机输出轴速度 | V0 bring-up 使用 RPM | RPM | 每圈计数未确认前不得输出伪 RPM |
| 车辆线速度 | m/s | m/s | 由 vehicle/kinematics 层负责换算 |
| 角速度 | rad/s | 界面可显示 deg/s | 算法内部统一 rad/s |
| 角度 | rad | 界面/舵机标定可用 degree | 算法内部统一 rad |
| PWM 命令 | 归一化 `[-1.0,+1.0]` | 可显示百分比 | timer count 只存在于硬件适配层 |
| 编码器 | 有符号 count/delta | count | 每圈计数必须注明解码模式 |
| 电压/电流 | V/A | V/A | 原始 ADC 值不得越过 power-monitor 边界 |

## 车辆坐标系与正方向

- 使用右手坐标系：`+x` 向前，`+y` 向左，`+z` 向上。
- 从上方看，逆时针 yaw 为正；正转向角表示向左转。
- `Left/Right` 以车辆朝前方向观察来定义。
- 经通道极性修正后，正电机命令必须产生车辆向前运动。
- 经通道极性修正后，正编码器增量必须表示车轮向前转动。
- 舵机脉宽与转向方向的对应关系由硬件配置决定；vehicle 层的正方向始终固定为向左。

## 命名与范围

- 板级适配层以上统一使用 `left/right`，不用含糊的 `motor1/motor2`。
- 接插件层可以保留 `A/B`、`E1/E2`，但到 `left/right` 的映射只能放在一处配置中。
- Duty 范围包含端点 `[-1.0,+1.0]`；非有限值或越界值必须拒绝或转入安全状态，不能回绕。
- 目标 RPM 使用项目配置的对称限幅。电机资料和实测完成前，该限幅为 `[UNKNOWN]`，闭环模式不得启用。
- `coast`、`short_brake`、`standby/disable` 是不同状态；使用“停止”一词时必须明确具体策略。

## 时间约定

- 控制周期基线为 10 ms（100 Hz），必须用 GPIO 波形实测。
- 遥测默认 20 Hz；只有证明不影响控制周期后才允许提高到 50 Hz。
- UART 命令解析和遥测格式化不得在控制 ISR 中执行。
- 所有 deadline 比较使用无符号差值，保证计数回绕安全。

## 状态标记

- `[PROJECT-CONTEXT]`：用户报告的库存/背景，不等于有厂商依据的电气/性能事实。
- `[SIMULATION-TESTED]`：指定模型/场景的仿真通过，不等于真实感知或实车通过。

- `[FACT]`：有厂商/原始资料位置支持的事实。
- `[DESIGN]`：项目已接受的工程设计，不是硬件事实。
- `[IMPLEMENTED]`：代码中已经实现。
- `[BUILD-VERIFIED]`：已用指定目标/工具链编译；不代表运行正确。
- `[HOST-TESTED]`：已通过指定电脑端测试；不代表硬件行为正确。
- `[TESTED]`：已通过记录在案的真实测试；必须写明测试。
- `[TESTED-BY-TEAM]`：团队此前报告已实测，但本轮未重复验证。
- `[UNVERIFIED]`：已有实现或说法，但尚未测试。
- `[EXPERIMENTAL]`：明确属于临时实验。
- `[UNKNOWN]`：没有可靠信息。

任何 Agent 都不得静默提升状态等级。

## 第一阶段坐标与数据边界（DEC-014，未来接口草案）

- `base_link` 的实体原点须随实际底盘确认、绘图并记录；现有仿真是阿克曼后轴中心，不能直接当实物已确定原点。
- `slot` 坐标草案：入口中点为原点，+x 从入口指向库内，+z 向上，+y 按右手系。它不等于车辆朝向；倒车入库时车头方向通常朝入口，目标 yaw 必须显式表达。
- `T_A_B` 表示将 B 系坐标变换到 A 系。位置 m、角度 rad；yaw 规范到 [-π,π)，不把图像像素直接当地面米数。
- 图像坐标/相机光学系单独命名，不能默认与车辆的前/左/上重合；内外参必须包含分辨率、标定版本和安装配置。
- 时间记录须注明采集/接收和时钟域；跨设备未同步的时间戳不可直接相减。运动相机姿态与图像时间配对。
- 开发与独立验收数据按场景划分，不能把同一段相邻帧随机拆开宣称跨场地泛化。原视频/个人隐私默认不公开。
- 仿真配置、实测配置和标定模板分开；UNKNOWN 不填“看起来合理”的数。只有评估器能使用仿真真值，不得冒充真实感知输入。
