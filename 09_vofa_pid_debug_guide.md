# 09 VOFA+ PI/PID 调试指南

只有编码器比例和控制周期都有效后，才能按本指南调速。在此之前只显示 raw count 和 duty，禁止把曲线标为 RPM。

## 建议显示的曲线

| 曲线 | 含义 | 重点观察 |
|---|---|---|
| `target_rpm` | 输出轴目标速度 | 阶跃/斜坡幅度与方向 |
| `actual_rpm` | 完成标定的实测速度 | 上升时间、噪声、滞后、超调、稳态误差 |
| `error_rpm` | target − actual | 正负方向、偏置、稳定过程、负载响应 |
| `controller_output` | PI/PID 归一化输出 | 饱和、振荡和解除饱和恢复 |
| `pwm_duty` | 经过限幅/slew 后真正应用的命令 | 安全层是否改变了控制器输出 |
| `encoder_delta` | 每个控制窗口的有符号原始计数 | 接线、噪声和丢脉冲迹象 |
| 可选 `bus_voltage` | 已标定的驱动输入电压 | 电压跌落是否与速度/负载相关 |
| `fault_flags` | 安全状态 | 输出为什么被关闭 |

## 调参前必须完成

- 正 duty 对应经过修正后的正 encoder delta。
- 调试 GPIO 已证明控制周期实际为 10 ms。
- 通过多圈测量确认 `counts_per_output_rev`。
- 根据真实硬件证据设置保守 duty 与 RPM 实验限幅。
- 车轮架空或使用安全夹具，并明确 `MOTOR:STOP` 的实际状态。
- Telemetry 位于 ISR 之外，连接或断开 VOFA+ 不会改变控制周期。

## 调参顺序

### 1. 开环

显示 `pwm_duty`、`encoder_delta`、`actual_rpm`，缓慢增加和减小 duty。记录死区、起转阈值、左右差异、电源电压和电流。RPM 未标定时不得推导控制器增益。

### 2. 只使用 P

设置 `Ki=0`、`Kd=0`，从较小 `Kp` 和低目标速度开始。

- 实际速度远离目标时立即停止：反馈方向错误。
- 响应缓慢且稳态误差较大时，逐步增加 `Kp`。
- 出现振荡或抖动时先降低 `Kp`；检查速度噪声和采样时序，不要立刻堆滤波器。
- P-only 存在一定稳态误差是正常现象。

### 3. 加入 I

保留已经稳定的 `Kp`，从很小的 `Ki` 开始。

- 只增加到足以消除持续稳态误差。
- 观察 `controller_output` 是否长期顶在边界；解除饱和后恢复很慢时，先检查 anti-windup，并降低目标或负载。
- 测试目标归零、disable/re-enable 和正反换向；积分必须复位或遵循已记录的切换策略。

### 4. 只有证据需要时才加入 D

如果 P/PI 已调好，但仍存在可重复的超调/动态误差，且速度信号足够干净，才尝试非零 `Kd`。小步调整并观察噪声放大。厂家默认增益的时基和单位不同，不能作为起点。

## 曲线异常的快速解释

| 现象 | 最可能原因 | 第一检查项 |
|---|---|---|
| 速度立即发散 | 反馈极性错误 | Motor/Encoder polarity |
| RPM 刚好 ×2 或 ÷2 | 采样周期或解码倍频错误 | GPIO 实测周期；Encoder Mode/每圈计数 |
| actual speed 抖动，raw delta 交替变化 | 窗口太短、量化或信号噪声 | 原始 A/B 波形、filter、估计窗口 |
| 输出长期停在 ±1 | 目标不可达、电压/电流受限或速度比例错误 | 开环最大值、母线电压/电流、RPM 公式 |
| 饱和后产生大超调 | 积分 windup | anti-windup 与 reset 策略 |
| 开始绘图后控制异常 | UART 阻塞或遥测过载 | 时序波形、遥测频率与 buffer |
| 一侧稳定，另一侧方向相反 | Left/Right 映射或 polarity 错误 | Board Config 与实物标签 |

## 每次调参必须保存

- 固件标识与硬件配置。
- 控制频率和遥测频率。
- 每圈计数及其取得方式。
- `Kp/Ki/Kd`、输出/积分边界、slew 和目标曲线。
- 电源电压/电流，以及车轮是否架空/带载。
- 导出的 VOFA+ 数据或截图。
- 通过/失败结论，以及下一次只修改的一个参数。

使用 `templates/TUNING_RECORD_TEMPLATE.md`。不能只保留一张没有曲线标签的截图。
