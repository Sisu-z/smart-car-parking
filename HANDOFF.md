# 当前交接记录

## 最新交接：2026-09-22，普通线自主泊车基线与 GitHub 首次发布

发布核验：公开仓库 https://github.com/Sisu-z/smart-car-parking 。代码基线提交 `8bc0d8c97cd20d25eeac6a37a0ae158d9b15e6e9`；本节的后续文档提交记录发布结果，不改变已测算法。main 为默认分支，API 返回 private=false，公开原文入口可读取，远端与本地基线提交匹配。

从 GitHub 全新克隆（不借用原工作区 build/.venv，子模块从上游取固定提交），按 README 执行 uv sync --frozen 和默认复跑：macOS、Python 3.13.13，7 项 C、9 项 Python、7 个仿真场景均通过；报告 hardware_tested=false、stm32_status=NOT_REQUESTED。默认未构建 MCU 是明确范围，不是跳过后冒充通过。本机另一环境 Python 3.12.13 完整运行含 STM32 配置/链接也通过。缺少 --baseline 却要求 MCU 编译时以参数错误退出，未悄悄使用本机私有路径。Markdown 本地链接检查与 git diff --check 通过。

### Current State — 当前状态

第一阶段目标更新为：普通车位线、随机位置/朝向主动找位、末段倒车入库、独立出库、跨场地展示。旧 DEC-010 已替代，当前 DEC-013～016 与总体方案 M0～M6 为准。现有软件没有因为改文档而新增这些能力；仍是离线实验和默认锁定的单电机候选。

### Changes — 本轮变更

- 重写唯一总体方案、短入口和下一步；同步上下文、库存/预算、决策、问题、接口草案、单位/坐标、模块边界、协作与任务模板。未新建另一套总方案。
- 去除旧 AprilTag 主线建议，但保留离线实验/回归；新增感知/任务草案与现行 v0.1 明确隔离；修正旧通信文档“斜坡停机/standby”与 bench 实际 coast/超时锁存的冲突。
- 新增公开 README 与发布忽略规则；外部资料/学习固件路径改成逻辑根目录，原件未移动或修改。厂商资料、外部 HAL、.venv/build/results、原采集数据与本地配置不上传。
- PythonRobotics 使用固定提交子模块，另保留许可证文本；电脑端默认复跑不再依赖私有 HAL。STM32 编译须显式 --with-stm32 --baseline；板端控制逻辑和安全门控未改。
- 初始化并发布 main 至公开仓库 Sisu-z/smart-car-parking。首次导入是实验基线归档，不代表控制/安全代码获得独立审查批准。

### Verified — 本轮实际验证

- 使用锁定依赖执行 tools/run_offline.py --with-stm32 --baseline（本机只读 test01）：C 配置/构建/测试、Python 测试、7 个仿真场景、STM32 配置/链接均退出成功；细节为本地 results/verification.json，不公开原始构建日志。
- 7 项 C 测试、9 项 Python 测试通过；C 开启地址/未定义行为检查。仿真仍是 3 个固定起点泊车 + 4 个故障场景，不是随机找位或跨场地验收。
- 暂存清单检查排除了虚拟环境、构建/结果目录、厂商包与嵌套 .git；高风险凭据模式/个人绝对路径检查没有命中待发布源码文档。这不是穷尽性安全/许可审计。

### Not Verified — 未验证

没有实物连接、烧录、串口控制、X5 部署、相机采集、普通线识别、随机找位或真实出库测试。相机方案未定，底盘结构与电流/CPR/供电仍有 UNKNOWN。未来接口只是设计，未声称新协议已实现。已验证当前 Mac 上干净克隆复跑，不承诺未经测试的 Windows/Linux/板端环境全部可用。

### Known Problems / Next Recommended Action

已知真正阻塞：硬件安全参数和实测、真实相机样本/视野、普通线位姿/空闲空间、正式通信与实物车辆模型。先按 NEXT_ACTIONS 两线推进；不要重新做标记路线或把原固定起点场景当主线完成。

每次后续任务：独立分支/工作目录，声明基础提交与模块边界；更新测试/未验证和交接，复审后由 Integrator 合入 main。当前未宣称设置了 GitHub 强制保护或定时后台维护。

---

## 历史交接：2026-09-22，硬件联调前软件资产（部分路线已被替代）

### Current State — 当前状态

本轮交付的是**可复跑的离线工程原型 + 默认锁定的单电机固件候选**，不是实车完成。日常只需打开 `results/离线验证结果.html`；构建测试记录在 `results/verification.json`。全部厂家源资料和原 `test01` 未修改，未烧录、未扫描/打开硬件端口、未部署 RDK。

运行：在本目录执行 `uv run python tools/run_offline.py`。依赖由 `uv.lock` 固定；每个步骤最多 120 秒，失败记录日志并继续不依赖它的步骤，不自动无限重试。外部依赖说明在 `third_party/README.md`。

### Changes — 做了什么、改了哪里

1. **现有 C 核心继续复用**：修正非法 duty 后仍保留旧目标、初始化失败仍保留可用状态、非有限数值/测速分母异常等问题。新增 `firmware/bench/` 安全编排与 `command/line_receiver.*` 有界分帧。
2. **候选固件已完整链接**：`firmware/platform/stm32f103_hal/{bench_main.c,CMakeLists.txt,arm-toolchain.cmake}` 只读复用旧 HAL/CMSIS/startup/linker，包含 Motor A + E1 + UART。PWM A 使用 CH4，未复制厂家 A/B 写反实现。Stop 明确采用芯片真值表的 coast，而不是用 PWM=0 冒充高阻。
3. **开源泊车链路**：固定 PythonRobotics 提交 `08b453a8e3b7a161b6fbbe6b73737758a8ec3482`，直接调用原版 Reeds–Shepp；`offline/parking.py` 做约束/碰撞筛选/Pure Pursuit 适配，`native_motor.py` 通过管道调用与 MCU 同源的 C Bench/PI，不另写一份 Python PI 冒充联调。
4. **视觉与排障工具**：`offline/vision.py` 调用 OpenCV 的 AprilTag 字典检测/IPPE，保留双解和拒绝条件；`tools/calibrate_camera.py` 调用棋盘标定；`tools/analyze_log.py` 分析 v0.1 遥测，区分候选原因而非武断判断堵转。
5. **结果可查看**：`offline/report.py` 生成中文离线回放页面；`results/` 保存场景 CSV、JSON、各步骤日志。用 Tabbit 检查过 7 个场景按钮、切换和滑块定位，浏览器无脚本错误；已检查截图。
6. 同步更新现有入口、项目状态、路线、接口、决策和未决问题，没有再创建一整套重复方案文档。具体代码/文档以本轮工作区为准，不含硬件事实升级。

### Verified — 这次确实跑过的验证

- `[HOST-TESTED]` **7 项 C 测试通过**，包含原有 5 项，加安全状态与数值边界；启用 AddressSanitizer/UndefinedBehaviorSanitizer，测试断言在发布配置也不被关掉。并非穷尽性安全证明。
- `[HOST-TESTED]` **9 项 Python 测试通过**：真实 C 进程断连停机且不自动恢复、车身碰撞、目标阻挡；合成标记检测/错误 ID/空图、投影位姿、双解拒绝、刚性坐标变换、日志 invalid 与时间回绕。
- `[SIMULATION-TESTED]` **7 个场景符合各自预期**：直线倒车、侧移泊车、垂直泊车完成；目标被占用拒绝规划；定位丢失、定位跳变、上位机断连触发相应故障并降至零 duty。除终态外还检查故障原因、响应截止时间和输出限幅，不以“出现任意故障”当通过。
- 三个成功场景末端位置误差约 0.0103/0.0109/0.0108 m，航向误差约 0/0.0292/0.0159 rad。**仅为这三个假设模型场景的结果，不能宣称实车厘米级精度，也不是成功率统计。**
- `[BUILD-VERIFIED]` Cortex-M3 ELF 完整链接（GNU Arm 15.2.1），不是只编译对象。最终体积见 `results/step_07.log`，使用原 64K Flash/20K RAM 链接布局；不能由链接成功推断运行栈、实时性或电气安全。
- Python 文件语法/视觉 CLI/标定 CLI 入口检查通过；棋盘标定尚无真实数据验收。

### Not Verified — 不能混淆的边界

- 真实 MG513XP28 电流、编码器每圈计数、实际底盘类型与尺寸、D153 PCB 版本、舵机和电源树都仍待确认。`offline` 的数值不是补齐这些 UNKNOWN 的证据。
- 仿真是二维阿克曼后轴模型、单等效驱动轴、一阶电机响应；无打滑/坡度/机械间隙/真实传感噪声建模。闭环观测来自模拟真值；**视觉离线算子未接入图像生成闭环，更未上真实相机/RDK。**
- 固件默认 `hardware_approved=0`、CPR=0、RPM 上限=0、增益=0；不能直接开电机。10% 软件 duty 也不是已确认的安全电流限制。
- STM32 的 UART 中断、GPIO 电平、PWM、定时周期、encoder 极性、真实负载/栈用量和物理停止尚未验证。当前使用 coast；没有独立硬件急停、过流/堵转检测或独立硬件看门狗，不得无人看护带载运行。
- v0.1 UART 文本协议依旧没有 CRC、序列号、会话握手；仅接收时刻续租，不能防止旧命令延迟到达。管道里的 C/T 注入前缀只供测试，不能发送给板端。
- RS 仅枚举几何候选并按离散车身碰撞过滤，不是任意障碍环境的完备搜索，不是连续扫掠安全证明；尚未实现出库、自动恢复、里程计融合或正式双轮车辆控制。

### Known Problems — 小坑与暂缓事项集中报告

本轮没有遗留需要继续重复重试的死循环。两个实现问题已在有限尝试内解决：

- 主机桥首轮遥测下标错误，启动时报 IndexError；统一到既有 10 字段协议后修正，集成测试通过。
- 垂直泊车首轮末段偏离，最近位置误差约 0.119 m，随后 TRACKING_ERROR 停止。原因是规划曲率贴执行器极限、换向未预先对齐转向。保留 15% 曲率余量并增加起步转向等待后通过，**没有放宽原验收误差/跟踪故障阈值**。

以下主动暂缓，不把它们算作完成，也没有为此反复消耗额度：

| 暂缓项 | 原因 | 下一次需要什么 |
|---|---|---|
| 实车 GPIO/电源/编码器/PID 验证 | 缺硬件身份和测量证据 | 按硬件清单确认、先无电机测波形 |
| 正式 RDK 传输协议与真实串口端到端测试 | 本轮集中保证 v0.1 台架链路；尚未做会话/序列/校验 | 在现有接口外单独实现并测试，不修改 v0.1 含义 |
| X5 部署、真实相机、标定位姿到车辆坐标 | 无已授权可访问的板端及真实相机标定输入 | 提供板端环境和图片/内外参；不猜 SSH 地址或标定值 |
| 出库、扰动批测与复杂障碍规划 | 超出本轮最小泊车回归集，缺实物运动学 | 先确认底盘，再有针对性扩场景；不用先装完整 ROS2/SLAM |
| 标定工具真实数据验收 | 没有棋盘图片 | 同分辨率多视角棋盘照片和实测格尺寸 |

### 【自主调整说明】

- 原计划偏向自写核心以便学习；改为开源规划/视觉 + 复用现有 C/HAL，学习性重写不是交付条件。
- 原路线把舵机迁移放在单电机之前；独立单电机候选不包含舵机，绕过这个依赖，保留旧工程；联合工程以后再做。
- 为减少重复依赖，用 OpenCV 的 AprilTag 字典检测/IPPE 完成离线工具，不再另外编译独立 AprilTag 库。
- 高级方案只做二维受控实验；没有安装完整 ROS2/SLAM 或生成机械/PCB。以少量可回归场景验证接口，不冒充真实车辆模型。

### Next Recommended Action — 下一位 Agent

先读本段和 `INTERFACE_CONTRACTS.md`，复跑入口，不要重写已取用的规划/视觉库。硬件就绪则从默认锁定的单电机候选开始审核；若硬件仍未就绪，最值得补的是独立的会话/序列号协议测试，以及真实相机样本的离线定位，不要继续凭仿真假设调“实车增益”。

---

## 上一阶段记录（历史，不是最新完成状态）

日期：2026-09-21

## 当前状态

- 硬件和参考资料审计已整理在 `01`～`05` 文档中。
- 当前学习固件仍位于 `<STM32_BASELINE_DIR>`。
- 该工程在 `main.c` 中实现了舵机 PWM、缓动、UART 行接收与两字段 VOFA+ 遥测。
- 可复用的纯 C motor、encoder/speed、PID、command、telemetry 模块已经独立实现，未写入 CubeMX 生成代码。

## 本阶段变更

- 新增项目总路线、共享上下文、决策与问题日志、统一约定、接口契约、模块边界及审查流程。
- 发现一个第一轮审计未明确指出的定时器冲突：当前舵机占用 TIM3/50 Hz，厂家电机参考占用 TIM3 CH3/CH4/10 kHz。
- 提出但尚未实机验证的调整：舵机迁移至 TIM1_CH1/PA8，TIM3 专用于双路 Motor PWM。

## 已验证

- `[FACT]` 当前 `.ioc`：STM32F103C8T6、72 MHz、TIM3_CH1 PA6，PSC=71/ARR=19999；USART1 PA9/PA10。[来源：`test01.ioc:8-27,107-135`]
- `[IMPLEMENTED]` 当前 `main.c`：`S:` parser、1000～2000 us 范围、缓动、50 ms 遥测、逐字节 UART 接收 ISR。[来源：`Core/Src/main.c:49-58,107-158,320-368`]
- `[FACT]` D153 引脚映射、5 ms/10 ms 冲突及 PWM A/B 冲突已记录在 `01`～`05`。
- `[HOST-TESTED]` 2026-09-21 通过 5 项主机测试：motor 映射/缓动、encoder 回绕/方向、RPM 换算、PI 限幅/复位、command/telemetry 解析与格式化。
- `[BUILD-VERIFIED]` 全部可复用模块已用 STM32 GNU Tools `arm-none-eabi-gcc 14.3.1` 编译为 Cortex-M3 对象文件，并将警告视为错误。
- `[BUILD-VERIFIED]` 未修改的 `test01` 重新执行现有 CMake 构建，结果为 `ninja: no work to do`；没有烧录或实机复测。

## 尚未验证

- 未执行电机/编码器实机测试、每圈计数标定、带载电流测试或 PI 硬件测试。
- 暂定定时器分配尚未重新生成和测量。
- RDK X5、摄像头、泊车定位和正式传输均未集成。

## 已知问题

- P0 未知项：电机电流、D153 版本、GMR 每圈计数和完整电源树。
- `test01` 主循环使用阻塞式 `HAL_UART_Transmit(..., HAL_MAX_DELAY)` 与 `HAL_Delay(10)`；适合当前舵机学习，不适合未来闭环架构。
- 当前代码集中在 `main.c`；应该在集成副本中逐步拆分，不应在下一次实机门槛前为了重构而重构。

## 下一步建议

直接执行 `NEXT_ACTIONS.md`：先登记准确硬件，再创建 `test01` 的保护副本，解决 CubeMX 定时器分配；完成无电机波形验证后才运行单电机 B01。
