# 03 电机 → 编码器 → 速度 → VOFA+ → PI/PID Bring-up 路线

本文件只定义逐级验活方法，不决定最终车型、整车功能、PCB 或机械结构。所有未写“来源”的阈值、频率与软件结构均为**测试建议**，不是硬件事实。

引用约定：来源根目录为 `<HARDWARE_SOURCE_ROOT>`；文中的“用户手册”指 `【WHEELTEC】TB6612电机驱动附送资料_V3.2_2026.07.25/1.用户手册与使用教程/电机驱动模块使用手册—TB6612(2025.08.27).pdf`；“GMR HAL 例程”指同资料包 `4.例程源码/1.STM32F1例程/2.D153B（双路驱动带稳压版）例程/D153B_GMR编码器HAL库例程_2026.8.20.zip`；“GMR 标准库例程”指同目录 `D153B_GMR编码器标准库例程_2025.7.7.zip`。压缩包引用随后给出包内文件与行号。

## 0. 上电前门禁

在以下条件全部满足前，不进入电机带载测试：

- 确认驱动板丝印版本；D153C V1.0 与 V1.1 的反接保护和编码器供电不同。[来源：`不同硬件版本更新内容.txt:1-5`；`D153C电机驱动原理图V1.0.pdf` p.1；`D153C电机驱动原理图V1.1.pdf` p.1]
- 确认电源正负极并共地。随货手册禁止电源反接、短路、静电触碰控制引脚和电机堵转。[来源：`TB6612稳压版随货手册.pdf` p.1]
- 先不接电机，测驱动板 5 V 与 3.3 V 测试点；厂家给出的快速判定范围为 5 V±0.5 V、3.3 V±0.3 V。[来源：`TB6612稳压版随货手册.pdf` p.1]
- 确认电机额定电压为 12 V 之外的关键数据仍为 `UNKNOWN`：额定电流、堵转电流、减速比、输出轴转速和 GMR 编码器精度。本地没有找到 `MG513XP28_12V` 精确 SKU 数据页。
- 首次转动时架空车轮或拆离传动负载，只接一路电机；使用可观察电流的限流电源。**限流值暂不写死**，因为电机额定/堵转电流没有可靠来源。
- 接线基线：AIN1=PB14、AIN2=PB15、PWMA=PB1/TIM3_CH4；BIN1=PB13、BIN2=PB12、PWMB=PB0/TIM3_CH3；E1A=PB6/TIM4_CH1、E1B=PB7/TIM4_CH2；E2A=PA0/TIM2_CH1、E2B=PA1/TIM2_CH2；ADC=PA6；USART1=PA9/PA10。[来源：`电机驱动模块使用手册—TB6612(2025.08.27).pdf` p.17；`D153C模块与STM32F103C8T6核心板接线说明.png`；厂家 GMR HAL 例程 `tb6612demo.ioc:66-88,162-182`]
- STBY 必须为高电平才进入工作状态；低电平为待机。[来源：`TB6612FNG芯片手册.pdf` p.2、p.4；用户手册 p.11]

## 1. 统一的软件测量口径

### 1.1 建议的时基

**项目建议**：速度环先使用 10 ms 固定周期，并以示波器/逻辑分析仪测量调试 GPIO 的实际周期。厂家例程不能作为 10 ms 证据：其 TIM1 配置在 72 MHz 下实际为 5 ms。[来源：GMR HAL 例程 `Core/Src/tim.c:46-70`、`tb6612demo.ioc:138-177`；标准库例程 `HAREWER/TIMER/timer.c:16-29`]

```text
实际周期 = (PSC + 1) × (ARR + 1) / TimerClock
```

如果使用 72 MHz 的 TIM1 计数时钟，10 ms 的一个可选配置是 `PSC=7199, ARR=99`。这是**拟定配置**，必须以当前时钟树和实测 GPIO 为准。

### 1.2 计数与速度公式

先把四个量分开记录，不允许只写一个模糊的“编码器精度”：

```text
PPR_sensor             传感器每电机轴转的基础脉冲数，UNKNOWN
decode_multiplier      解码倍频，待由定时器模式和实测确认
gear_ratio             电机轴到输出轴减速比，UNKNOWN
counts_per_output_rev  每输出轴一圈的实际计数，必须实测确认
```

计算式：

```text
rpm = delta_count × 60 / (counts_per_output_rev × sample_period_s)
```

厂家手册只给出包含“采样频率、倍频、减速比、编码器精度”的换算思路，例程本身没有完成 RPM 换算。[来源：用户手册 p.23、p.27]

## 2. 逐级 Bring-up

### 当前工程的 CubeMX 前置调整

现有 `test01` 的 PA6/TIM3_CH1 以 PSC=71、ARR=19999 产生 50 Hz 舵机 PWM。[来源：`<STM32_BASELINE_DIR>/test01.ioc:21,51,128-133`；`Core/Src/main.c:223-250`] D153 电机参考则要求 TIM3 CH3/PB0、CH4/PB1 共用 10 kHz 基频。[来源：用户手册 p.11、p.17；GMR HAL 例程 `Core/Src/tim.c:133-173`] 因此不能直接把两个电机通道加到当前 TIM3。

第一候选配置（`[DESIGN][UNVERIFIED]`）：

| 功能 | CubeMX 配置 |
|---|---|
| Servo | TIM1_CH1/PA8；1 MHz 计数，ARR=19999，50 Hz |
| Motor A/B PWM | TIM3_CH4/PB1、CH3/PB0；PSC=0、ARR=7199，10 kHz |
| Direction | PB12、PB13、PB14、PB15 推挽输出 |
| Encoder E2 | TIM2_CH1/CH2 PA0/PA1，Encoder Mode，16 位满周期 |
| Encoder E1 | TIM4_CH1/CH2 PB6/PB7，Encoder Mode，16 位满周期 |
| UART | USART1 PA9/PA10，115200 8N1 |
| Control scheduler | 先用非阻塞 1 ms tick 派生 10 ms flag；调试 GPIO 实测 |

执行顺序不是一次全开：先迁移并回归 servo/UART，再加断电波形，再接一台电机，再接一个编码器。

### B01：单路电机开环冒烟测试

- 前置条件：通过第 0 节；车轮架空；只接 Motor A；STBY 高；编码器暂不参与控制。
- 外设/引脚：PB14/PB15 方向，PB1/TIM3_CH4 PWM。厂家建议 PWM 频率 10 kHz。[来源：用户手册 p.11、p.17]
- CubeMX/代码：启用 TIM3_CH4 PWM 与 PB14/PB15 GPIO；启动通道后使用 `firmware/motor/` 生成 `MotorOutput`，由薄 HAL 适配写 CH4 和方向脚。不要复制厂家 `Set_PWM`。
- 操作：从 0 开始缓慢增加占空比，短时观察静止、起转、匀速、归零；不要直接阶跃到满占空比。
- 验证：PWM 引脚频率与占空比、供电电压、空载电流、噪声和发热。
- 通过标准：命令为 0 时电机静止；小幅增加命令后可重复起转；归零后立即回到所选停机状态；无异常电流、异味或快速升温。
- 常见故障：STBY 未拉高；A/B 端口接反；PWM 写错 TIM3 通道；电源压降；方向脚组合错误。
- 下一依赖：只有这一步通过，才做正反转。

### B02：正反转与三种停机状态

- 前置条件：B01 通过。
- 外设/引脚：同 B01；Motor B 对应 PB13/PB12 + PB0/TIM3_CH3。[来源：用户手册 p.17]
- CubeMX/代码：仅在 A 通过后启用 CH3 和 B 方向脚；HAL 适配必须为 coast、short brake、standby 写出独立真值表分支。
- 操作：分别验证 CW、CCW、short brake、coast/standby；换向前先降到 0，再经过短暂停机窗口后反向。这一换向策略是**项目建议**。
- 验证：命令符号、轮子物理方向、编码器符号暂时分别记录，不要在此步强行统一。
- 通过标准：同一绝对占空比下可稳定双向运行；0 命令不会意外进入反向驱动；明确知道每一种停机 API 对应的 TB6612 真值表状态。
- 常见故障：厂家 HAL 例程的 `Set_PWM` 将 PWMA 写入 CCR3、PWMB 写入 CCR4，与接线表相反。[来源：GMR HAL 例程 `Core/Src/tim.c:423-445`；标准库例程 `HAREWER/PWM/pwm.c:46-70`]
- 下一依赖：固定方向映射后才能建立一致的速度正负号。

### B03：PWM 占空比扫描

- 前置条件：B02 通过；仍然架空、单电机。
- CubeMX/代码：无需新增外设；使用 `Motor_SetTargetDuty()` 和 `Motor_Update()` 的归一化命令与 slew，实验层再施加更低的临时 duty 上限。
- 操作：按离散档位逐步上升再下降；每档记录命令、实际占空比、电源电压、电流、是否起转和稳态转速原始计数。
- 验证：死区、起转阈值、上下行滞后、饱和区和左右电机差异。
- 通过标准：输出限幅有效；任意非法输入都被钳位；命令回零可靠；曲线无突发反向。
- 常见故障：直接使用厂家 `±7200` 魔数却没有把它和 `ARR=7199` 封装为配置；换板或改 PWM 分辨率后失配。[来源：GMR HAL 例程 `Core/Src/tim.c:133-173,452-481`]
- 下一依赖：得到可安全用于编码器验证的低、中档 PWM。

### B04：编码器原始计数

- 前置条件：确认编码器供电与板版本；先用手转轮，不使能电机。
- 外设/引脚：E1=TIM4 PB6/PB7，E2=TIM2 PA0/PA1。[来源：用户手册 p.17；GMR HAL 例程 `tb6612demo.ioc:66-88`]
- CubeMX/代码：一次只启用一组定时器 Encoder Mode；ARR=65535；初始 filter/mode 作为实验参数，不把 HAL 示例的 TI1/filter0 当定论。读取当前 CNT 后交给 `Encoder_UpdateCounter()`。
- 操作：读 CNT 原值与固定窗口 `delta_count`；每次仅转一个轮；正转一圈、反转一圈、多圈重复。
- 验证：静止噪声、单调变化、反向符号、每圈计数重复性和 16 位回绕。
- 通过标准：静止时计数不持续漂移；同方向计数符号稳定；同样圈数的计数重复性可接受；回绕处理测试通过。
- 常见故障：A/B 松线或交换；输入浮空；供电电平不匹配；HAL 示例滤波为 0 而标准库为 10；TI1/TI12 配置差异。[来源：GMR HAL 例程 `Core/Src/tim.c:73-117,177-221`；GMR 标准库例程 `HAREWER/ENCODER/encoder.c:12-75`]
- 下一依赖：只有原始计数可靠，才标定方向和 `counts_per_output_rev`。

### B05：方向统一与计数标定

- 前置条件：B02、B04 通过。
- CubeMX/代码：无需改变外设；只更新集中式 `motor_polarity`、`encoder_polarity` 与校准记录，不在业务代码散布负号。
- 操作：定义“车体前进”为正；为每路电机保存 `motor_polarity` 与 `encoder_polarity`，不要散落负号。手动标记输出轴，低速转精确整数圈并统计计数。
- 验证：电机正命令时，经极性修正后的编码器计数也为正；正反各 N 圈的绝对计数接近。
- 通过标准：两路“前进”都报告正计数；获得带实验记录的 `counts_per_output_rev` 初值。
- 常见故障：仅把电机线互换却忘了编码器符号；把 GMR 默认目标 800 当作每圈计数；把霍尔例程目标 20 用于 GMR。[来源：GMR/Hall HAL 例程各自 `Core/Src/main.c:49-51`；`说明文档（必看）.txt:1-4`]
- 下一依赖：速度换算。

### B06：固定周期速度估计

- 前置条件：B05 完成；控制周期已经 GPIO 实测。
- CubeMX/代码：先用 1 ms 系统 tick 置位 10 ms 控制 flag，主循环无阻塞地消费；若实测抖动不满足，再引入专用定时中断。调用 `Encoder_DeltaToRpm()` 时传实测 `dt_s`。
- 操作：ISR 只做计数快照、时间戳和控制运算；不得在 ISR 中打印。主循环读取快照并发送。
- 验证：用时间戳检测周期抖动；比较不同采样窗下的噪声；必要时使用轻量低通滤波，但保留未滤波值用于诊断。
- 通过标准：实测周期与配置一致；没有串口打印造成的周期拉长；静止速度接近 0；恒定 PWM 时速度序列稳定。
- 常见故障：厂家回调直接 `printf`，而其 `fputc` 忙等 TXE/TC。[来源：GMR HAL 例程 `Core/Src/main.c:168-207`、`Core/Src/usart.c:42-47`]
- 下一依赖：物理单位换算与 VOFA+。

### B07：RPM 换算

- 前置条件：有效的 `counts_per_output_rev` 和 `sample_period_s`。
- CubeMX/代码：无新增配置；保持 `counts_per_output_rev` 未设置时返回校准错误，禁止以 0 或示例目标替代。
- 操作：按第 1.2 节公式输出 `rpm_raw`；对正反转、不同速度和不同窗口交叉验证。若条件允许，用外部转速计核验；没有外部仪表时明确标为“内部一致性验证”。
- 通过标准：输出轴转一圈对应的累计计数与标定值一致；改变采样周期后，稳态 RPM 不应按比例错误变化；正反符号正确。
- 常见故障：把 5 ms 当 10 ms会使计算结果差 2 倍；重复乘四倍频或漏乘减速比。
- 下一依赖：遥测和闭环。

### B08：VOFA+ 速度遥测

- 前置条件：B07 通过；USART1 连接稳定。厂家例程配置为 115200 8N1。[来源：GMR HAL 例程 `Core/Src/usart.c:55-72,95-108`；`使用说明.txt:1-4`]
- CubeMX/代码：保留现有 USART1 RX 中断；使用 `firmware/command` 和 `firmware/telemetry`。先沿用主循环发送，但取消 `HAL_MAX_DELAY`，并测最坏发送时间；需要时再改 DMA。
- 项目建议帧：`timestamp_ms,target_rpm,rpm_left,rpm_right,pwm_left,pwm_right,bus_voltage,state,faults\n`。
- 操作：控制环每周期更新数据，遥测以较低频率在主循环/DMA 发送；建议初始 20–50 Hz，具体值由链路负载实测决定。
- 通过标准：断开 VOFA+ 不影响控制周期；字段单位明确；数据丢失不会改变电机命令；图中阶跃与采样时间一致。
- 常见故障：在 ISR 内格式化浮点或阻塞发送；列顺序改变但上位机曲线标签未更新。
- 下一依赖：P 控制。

### B09：P 控制

- 前置条件：开环安全、RPM 可信、输出限幅和故障停机已生效。
- CubeMX/代码：无新增外设；`PID` 配置先令 `ki=kd=0`，输出交给 motor 模块，状态机在未 enable/校准/fresh 时拒绝闭环。
- 操作：从小 `Kp` 开始，单电机、低目标 RPM；记录目标、实测、误差、控制量和饱和标志。目标使用斜坡，不直接大阶跃。
- 通过标准：负反馈方向正确；增大正误差时控制量向正方向变化；系统可接近目标且不持续发散。
- 常见故障：编码器极性相反导致正反馈 runaway。厂家排障文档建议对编码器值或 PWM 取反，但项目中应通过统一极性配置解决。[来源：`TB6612带稳压模块问题排查和检测方法(2025.07.10).pdf` p.8]
- 下一依赖：PI 消除稳态误差。

### B10：PI，再决定是否需要 PID

- 前置条件：B09 稳定。
- CubeMX/代码：只更新参数，不重新生成外设；使用 `PID_Reset()` 覆盖 disable、fault 和定义的换向边界。
- 操作：保持 `Kp`，从小 `Ki` 增加；每次只改一个参数；覆盖起步、低速、负载变化、正反切换和目标归零。厂家代码名为 `Incremental_PI`，本质是增量 PI，不包含 D 项。[来源：GMR HAL 例程 `Core/Src/tim.c:452-481`]
- 通过标准：稳态误差明显小于 P-only；无持续振荡；归零与换向时积分状态正确复位/冻结；解除饱和后能及时恢复。
- PID 判定：只有在 PI 已验证、速度估计噪声可控，且确有可重复的过冲/动态问题时才试 D；D 项不是默认必需。
- 常见故障：直接沿用厂家 `Kp=0.2, Ki=0.1` 或霍尔版 `Kp=1, Ki=3`；这些参数绑定其错误采样周期和原始计数单位，不可迁移。[来源：GMR/Hall HAL 例程 `Core/Src/main.c:49-51`]
- 下一依赖：完整饱和与恢复测试。

### B11：输出限幅与 anti-windup

- 前置条件：PI 能稳定运行。
- 代码：`firmware/pid` 已实现输出限幅和条件积分的 host-tested 基线；硬件测试仍需验证目标不可达和解除饱和两种情形。
- 实现建议：输出统一钳位到 `[-pwm_max,+pwm_max]`；采用条件积分或 back-calculation；在 disabled、fault、目标归零和方向切换时按策略重置积分；加入目标斜坡。
- 验证：命令超过可达速度保持一段时间后降回正常范围；检查解除饱和是否快速恢复且不过度反冲。
- 通过标准：控制量永不越界；饱和期间积分不继续无界增长；解除饱和后在预期时间内恢复。
- 厂家边界：示例仅对增量 PI 的输出状态做 ±7200 钳位，没有显式条件积分或 back-calculation。[来源：GMR HAL 例程 `Core/Src/tim.c:459-479`]
- 下一依赖：可选前馈与双电机一致性。

### B12：前馈（可选）

- 前置条件：B03 已得到 PWM→RPM 开环曲线，B11 通过。
- 代码：现在不预写前馈实现；先保留接口空间和开环数据，只有曲线稳定可重复时再加分段模型。
- 项目建议：按正反方向分别拟合最小起转补偿与线性/分段前馈，PI 只修正残差。参数必须来自当前电源、电机、轮组和负载实测。
- 通过标准：相同目标下误差更小、PI 输出负担下降，且低速不会因死区补偿跳变。
- 不适用条件：电池电压变化、负载差异或死区尚未测量；此时先保留纯 PI。

## 3. 每一步必须保存的证据

建议每个测试保存一条版本化记录：

```text
测试编号 / 日期 / 固件提交
驱动板版本与照片
电机标签与编码器标签
供电设定与实际电压、电流
引脚映射 / PWM 频率 / 控制周期实测
命令序列
原始计数、RPM、PWM、故障标志
通过/失败及异常现象
下一步是否获准
```

“编译通过、串口有输出、轮子转了”都不是闭环通过标准；必须保留周期、方向、计数比例、饱和和故障恢复证据。
