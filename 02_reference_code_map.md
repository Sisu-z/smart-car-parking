# 02 厂家教程与参考代码复用地图

结论先行：**没有一份厂家应用层例程适合整包直接作为项目主工程。** 最值得保留的是 D153C/D153B 的 STM32F103C8T6 HAL 外设配置和接线映射；电机抽象、编码器采样、控制周期、速度换算、PI、命令与遥测应重写。厂家例程仍很有价值，但用途是“核对引脚与建立最小验证”，不是最终架构。

## 1. 资料覆盖范围

### 1.1 本地代码包

| 编号 | 厂家文件 | 目标与内容 | 与当前 STM32F103C8T6+HAL 的关系 | 结论 |
|---|---|---|---|---|
| E01 | `2.编码器的使用教程与测速原理/编码器例程/1.STM32F1例程.zip` | 标准库；TIM4 AB 相计数，200 ms 读取并串口打印 | MCU 家族相同，库体系不同；只有一路编码器 | `借鉴重写`。编码器模式思路可用；200 ms 阻塞延时和手工溢出判断不进入最终代码。[内部位置：`HARDWARE/ENCODER.c:3-39`、`USER/main.c:5-15`] |
| E02 | `.../编码器例程/2.ArduinoUNO例程（2024.04.15）.zip` | Arduino 编码器示例 | 平台不同 | `不用作 STM32 实现`；仅可辅助理解信号。压缩包内中文路径存在编码损坏，不作为核心证据 |
| E03 | `4.例程源码/.../D103A_HAL库例程_2026.8.20.zip` | 单电机开环；HAL；TIM4 PWM | C8T6+HAL，但 D103A 无板载编码器/稳压接口 | `可作单路 H 桥冒烟测试参考`，不作 D153C 双电机工程骨架。[内部位置：`tb6612_demo/tb6612_demo.ioc:14-26,122-126`、`Core/Src/tim.c:142-155`] |
| E04 | `.../D103A_标准库例程_2025.7.7.zip` | 单电机开环；标准库 | 库体系不同、板型不同 | `不用作主工程`；只参考真值表和最小转向流程 |
| E05 | `.../D153B_GMR编码器HAL库例程_2026.8.20.zip` | 双电机、GMR、ADC、USART、开环/闭环；C8T6 HAL | 最接近当前项目 | `核心参考，但应用层重写`。详见第 3 节 |
| E06 | `.../D153B_GMR编码器标准库例程_2025.7.7.zip` | E05 的标准库版本 | 引脚对应清楚，可用来交叉核对 HAL 端口 | `交叉验证用`。它揭示 HAL 版 PWM A/B 通道写反问题；不直接合入 HAL 工程。[内部位置：`HAREWER/PWM/pwm.c:46-70`] |
| E07 | `.../D153B_霍尔编码器HAL库例程_2026.8.20.zip` | 与 GMR HAL 实现相同，默认目标和 PI 参数不同 | 当前是 GMR，不应使用霍尔默认量程 | `不用其默认参数`；可用于证明厂家将传感器差异简化成目标/增益差异。[内部位置：`Core/Src/main.c:49-51`] |
| E08 | `.../D153B_霍尔编码器标准库例程_2025.7.7.zip` | 霍尔、标准库 | 平台/传感器默认参数都不匹配 | `不用作实现` |
| E09 | `.../D24A_GMR编码器HAL库例程_2026.8.20.zip` | 四电机；STM32F103RCT6；TIM5 PWM，TIM2/3/4/8 编码器 | C8T6 不具备该例程的封装与资源映射 | `不兼容，禁止直接移植`。[内部位置：`TB6612/TB6612.ioc:14-28,75-85,110-111,201-219`] |
| E10 | `.../D24A_GMR编码器标准库例程_2025.7.8.zip` | 四电机标准库 | 同上且库体系不同 | `不用` |
| E11 | `.../D24A_霍尔编码器HAL库例程_2026.8.20.zip` | 四电机霍尔 HAL | 同上 | `不用` |
| E12 | `.../D24A_霍尔编码器标准库例程_2025.7.8.zip` | 四电机霍尔标准库 | 同上 | `不用` |
| E13 | `4.例程源码/2.ArduinoUNO例程/TB6612motor_ArduinoUNO_Demo/TB6612motor_ArduinoUNO_Demo.ino` | Arduino 双路 TB6612 演示 | 非 STM32 | `概念参考`，不移植 |

厂家手册自己也说明 D24A 例程基于 STM32F103RCT6，四电机闭环使用 TIM5 产生四路 PWM、TIM2/3/4/8 读编码器；这不是 C8T6 的等价工程。[来源：`电机驱动模块使用手册—TB6612(2025.08.27).pdf`，p.15，4 节]

### 1.2 教程、图纸与视频

| 资料 | 可复用价值 | 使用方式 |
|---|---|---|
| `电机驱动模块使用手册—TB6612(2025.08.27).pdf` | TB6612 真值表、D153 接线、例程结构、速度换算缺口 | `高`：作为接线与边界主索引；芯片极限仍回到 Toshiba 数据手册 |
| `TB6612带稳压模块问题排查和检测方法(2025.07.10).pdf` | 无程序检测、电压轨检查、编码器导通、方向反号故障 | `高`：转成 bring-up 故障树 |
| `TB6612稳压版随货手册.pdf` | 快速禁令与安全检查 | `高`，但反接保护结论受板版本影响 |
| `D153C模块与STM32F103C8T6核心板接线说明.png` | 一张图覆盖两电机、编码器、ADC、STBY、5V/GND | `高`，但需与 HAL 代码交叉核对 |
| `编码器波形与原理简述.png`、`编码器无示波器情况排查方法.png` | AB 相概念、供电和最小排障 | `高`，不能提供 CPR/PPR |
| `TB6612带稳压电机驱动模块（D153C）使用教程视频.mp4` | 接线演示 | `辅助`：书面手册已覆盖主要事实；不作为唯一证据 |
| `2.编码器的使用教程与测速原理.mp4` | 编码器概念/测速教程 | `辅助`：精确参数仍需型号数据页或实测 |
| `直流电机原理视频教程.avi` | 入门原理 | `低`：不影响当前接口与代码决策 |
| R3X 各车型安装视频 | 机械装配顺序 | `后置`：等实际底盘型号确定后再用；不参与当前固件冻结 |
| R3X PDF/DWG/STP | 孔位、尺寸、机械安装 | `机械参考`：不据此选择最终车型 |

`WHEELTEC R3X系列底盘客户资料/智能小车底盘开发参考资料.txt` 只包含百度网盘链接。本次访问能够确认分享名，但文件列表接口要求验证，资料未落地到本地，因此其中可能存在的底盘控制源码**没有被审计**，不能写成“已覆盖”。

## 2. 功能复用地图

| 功能 | 最相关来源 | 厂家实现 | 兼容性 | 建议 | 风险/缺口 |
|---|---|---|---|---|---|
| TB6612 正反转/停机 | Toshiba 数据手册 p.4；E05 `tim.c:423-445` | IN1/IN2 真值 + PWM | 电气概念兼容 | `真值表直接采用；函数重写` | E05 对 `PWM==0` 仍设置反向 IN 状态；无显式 coast/brake/standby API |
| 10 kHz PWM | 用户手册 p.11；E05 `tim.c:133-173` | TIM3 CH3/CH4，ARR=7199 | C8T6 兼容 | `外设配置可再生成` | E05 `Set_PWM` 把 A/B CCR 写反；缺输入限幅 |
| 两路方向 GPIO | 用户手册 p.17；E05 `gpio.c:42-60` | PB12-15 推挽输出 | C8T6 兼容 | `可按表配置` | 实车左右电机安装方向不同，逻辑方向仍需校准 |
| AB 相硬件解码 | E05 `tim.c:73-117,177-221`；E06 `encoder.c:12-75` | TIM2/TIM4 Encoder Mode | C8T6 兼容 | `配置思路复用，读数 API 重写` | HAL 用 `TIM_ENCODERMODE_TI1`，标准库用 `TI12`；输入滤波 HAL=0、标准库=10，需实测选定 |
| 原始计数读取 | E05 `tim.c:410-420` | `(short)CNT` 后清零 | C8T6 兼容 | `小函数可借鉴，但封装/原子性重写` | 依赖固定采样窗；16 位溢出和并发需明确 |
| 固定周期测速 | E05 `tim.c:46-70`、`main.c:168-209` | TIM1 回调中采样 | 参数错误 | `重写` | 声称 10 ms，实际 5 ms；回调不检查 `htim->Instance` |
| RPM/线速度 | 用户手册 p.23、p.27 | 只给公式文字，没有实现 | 不能直接使用 | `重写` | CPR、倍频、减速比、轮径均需明确来源 |
| 开环 PWM | E05 `main.c:172-188` | 固定 3000/7200 | 可作冒烟参考 | `最小测试可借鉴` | 没有斜坡、超时、限流、指令层 |
| PI 闭环 | E05 `tim.c:452-481`、`main.c:191-208` | 增量式 PI + 输出钳位 | 算法概念兼容 | `重写` | 以原始窗计数为单位；示例周期错误；无模式复位/失联停机/结构化参数 |
| anti-windup | E05 `tim.c:459-479` | 输出状态直接钳位在 ±7200 | 仅有最弱形式 | `重写并显式测试` | 没有条件积分或 back-calculation；目标突变/饱和恢复未验证 |
| ADC 电源监测 | 用户手册 p.8；E05 `adc.c:126-160`、`main.c:112-113` | 1/11 分压，10 次平均 | C8T6 兼容 | `比例公式可用，采样调度重写` | `HAL_Delay(5)` 循环阻塞；ADC 参考和分压误差未校准 |
| UART 调试 | E05 `usart.c:55-72,95-108` | USART1 115200 8N1 | C8T6 兼容 | `配置可用` | `fputc` 忙等，且厂家从 ISR 调用 `printf` |
| VOFA+ | 本地没有专用例程 | 无 | — | `新实现` | 需要统一帧格式、节流、丢包/堵塞策略 |
| 串口在线命令 | 本地没有解析器 | 无 | — | `新实现` | 必须有范围检查、超时停机、错误响应 |
| 四电机控制 | E09-E12 | F103RCT6 + TIM5/TIM8 | 不兼容 C8T6 参考设计 | `当前不用` | 不应因 D24A 例程存在就承诺 C8T6 四闭环 |
| 舵机 | R3X 阿克曼 `注意事项.txt` | 0.5-2.5 ms、1.5 ms 中位 | 逻辑可独立复用 | `保留现有已验证舵机模块` | 型号和供电电流 UNKNOWN；与电机速度环分离 |

## 3. D153 GMR HAL 例程专项审计

### 3.1 可以保留的部分

1. `.ioc` 中 MCU、时钟和外设选择可作为再生成参考：TIM2/TIM4 编码器、TIM3 两路 PWM、TIM1 周期触发、ADC1_IN6、USART1。[来源：E05 `tb6612demo/tb6612demo.ioc:14-27,66-88,137-183`]
2. 方向 GPIO 的四个管脚与厂家接线表一致。[来源：E05 `Core/Src/gpio.c:52-60`；用户手册 p.17]
3. 读取 16 位计数器并转成有符号增量的核心思想可用。[来源：E05 `Core/Src/tim.c:410-420`]
4. ADC 按 1/11 分压换算的比例与板级原理图一致。[来源：E05 `Core/Src/main.c:112-113`；D153C V1.1 原理图 p.1]
5. USART1 115200 8N1 与接线说明一致。[来源：E05 `Core/Src/usart.c:55-72,95-108`；外层 `使用说明.txt`]

### 3.2 必须修正或重写的部分

#### 缺陷 A：PWM A/B 通道写反

- 接线表/接线图：PWMA=PB1=TIM3_CH4，PWMB=PB0=TIM3_CH3。[来源：用户手册 p.17；接线 PNG；E05 `.ioc:77-78,166-169`]
- 标准库版也按 `CCR4←PWMA`、`CCR3←PWMB`。[来源：E06 `HAREWER/PWM/pwm.c:69-70`]
- HAL 版却按 `CCR3←PWMA`、`CCR4←PWMB`。[来源：E05 `Core/Src/tim.c:443-444`]

判定：`VERIFIED BUG/CONFLICT`。HAL 应用层的 `Set_PWM` 不可直接复用。

#### 缺陷 B：“10 ms”实际为 5 ms

E05 在 72 MHz TIM1 时钟下设置 PSC=49、ARR=7199：

```text
T = (PSC+1)*(ARR+1)/72,000,000
  = 50*7200/72,000,000
  = 0.005 s
```

标准库 E06 的 PSC=7199、ARR=49 乘积相同，也为 5 ms；但函数名、注释和使用说明均写 10 ms。[来源：E05 `Core/Src/tim.c:46-70`、`.ioc:138-177`、`Core/Src/main.c:179-185`；E06 `HAREWER/TIMER/timer.c:16-29,66-72`；E06 `使用说明.txt:1`]

判定：`VERIFIED BUG/CONFLICT`。必须重新配置并测量控制周期。

#### 缺陷 C：ISR 做阻塞打印

`HAL_TIM_PeriodElapsedCallback` 每次做计数、控制；累计后直接两次 `printf`。`fputc` 对 TXE/TC 标志忙等发送。[来源：E05 `Core/Src/main.c:168-207`、`Core/Src/usart.c:42-47`]

判定：`不适合闭环主工程`。ISR 只写快照/环形缓冲；发送放主循环或 DMA 任务。

#### 缺陷 D：控制量不是 RPM

GMR 默认目标是 800 counts/window，霍尔默认目标是 20 counts/window；厂家手册明确说例程没有把编码器值转换成速度。[来源：E05 与 E07 `Core/Src/main.c:49-51`；用户手册 p.23]

判定：`演示参数，不可跨电机复用`。先标定 `counts_per_output_rev`，再用 rpm/rad/s 作为控制单位。

#### 缺陷 E：模式和安全状态不足

`Set_PWM(0,0)` 会落入反向分支，只是占空比为 0；没有显式 `coast`、`short_brake`、`standby`、斜率限制和通信超时停机。[来源：E05 `Core/Src/tim.c:423-445`；TB6612 数据手册 p.4]

判定：`必须重写 motor driver API`。

## 4. 直接复用、借鉴重写、禁止使用清单

### 可直接复用（仍需核对目标工程）

- STM32 官方 HAL/CMSIS 库文件：仅在许可证、版本和目标芯片一致时使用；更推荐从当前 CubeMX 工程重新生成。
- 厂家真值表、电压/电流边界、接线表：作为配置输入，不作为可执行代码；板版本需先确认。
- USART1 的 115200 8N1 参数、ADC 1/11 比例、10 kHz PWM 目标：可进入项目配置，但必须在实物上验证。

### 借鉴后重写

- TIM2/TIM4 Encoder Mode 配置。
- 有符号计数增量读取。
- 方向 GPIO 与 PWM 输出逻辑。
- 电压采样与标定。
- 增量式 PI 的概念。

### 禁止直接进入主工程

- E05/E07 的 `Set_PWM`（A/B PWM 通道映射冲突）。
- E05/E06 的控制周期参数（5 ms 被标成 10 ms）。
- ISR 内 `printf`。
- 厂家默认目标计数和 Kp/Ki。
- D24A/F103RCT6 的 TIM5/TIM8 配置用于 F103C8T6。
- 标准库工程、OBJ/HEX/AXF 和预编译对象直接混入 HAL/CMake 工程。
- 把“例程能编译/烧录”当作电机电流、编码器比例或闭环稳定性已经验证。

## 5. 后续补入资料后的增量审计入口

需要补到本目录或单独资料包后再审计：

1. MG513XP28_12V GMR 精确 SKU 数据页。
2. 百度分享中的“WHEELTEC 智能小车底盘开发参考资料”本地副本。
3. RDK X5 官方硬件接口/供电资料。
4. 舵机精确型号数据页。
5. 实物 D153C 正反面照片与版本号。

## 6. 当前 `test01` 工程审查

当前工程：`<STM32_BASELINE_DIR>`

| 内容 | 当前证据 | 复用判断 |
|---|---|---|
| MCU/时钟 | F103C8T6，HSE×9=72 MHz。[来源：`test01.ioc:8-17,107-125`；`Core/Src/main.c:170-203`] | `直接保留为基线` |
| Servo PWM | PA6/TIM3_CH1，PSC=71、ARR=19999，即 1 MHz 计数和 50 Hz 周期。[来源：`test01.ioc:21,51,128-133`；`main.c:210-250`] | `行为复用、资源需迁移`。与电机所需 TIM3 10 kHz 冲突 |
| Servo 命令 | `S:<value>`，只接受 1000–2000 us。[来源：`main.c:120-130`] | `直接保留兼容`，精确舵机安全范围仍待型号确认 |
| Slew | 主循环每次向目标移动 10 us，循环末尾 `HAL_Delay(10)`。[来源：`main.c:134,158,320-333`] | `借鉴重写`。把步长与实际 dt 显式化，避免被主循环阻塞改变速度 |
| UART RX | USART1 PA9/PA10，115200 8N1；逐字节中断组装换行命令。[来源：`test01.ioc:43,45-53`；`main.c:269-277,335-368`] | `结构可借鉴`；解析移入 `command`，ISR 只收字节 |
| Telemetry | 每 50 ms 输出 `servo_target,servo_current`，阻塞 `HAL_UART_Transmit(...HAL_MAX_DELAY)`。[来源：`main.c:136-155`] | `协议作为回归基线；发送实现重写` |
| Build | CMake 目标可增量构建；2026-09-21 复核为 `ninja: no work to do` | `[BUILD-VERIFIED]`，不是烧录/硬件验证 |

### 新发现的资源冲突

同一个定时器的所有 PWM 通道共享 PSC/ARR。当前舵机使用 TIM3 50 Hz，而 D153 双电机参考使用 TIM3 CH3/CH4 10 kHz；不能把厂家 CH3/CH4 直接加进当前 TIM3 配置。项目的第一候选调整是舵机移到 TIM1_CH1/PA8，TIM3 专用于电机，TIM2/TIM4 专用于两个编码器，USART1 保留。该分配是 `[DESIGN][UNVERIFIED]`，详见 `DECISIONS.md` DEC-009。

### 当前工程与厂家例程的合并策略

1. 不覆盖 `test01`。
2. 在保护副本中先迁移舵机并完成原有 VOFA+ 回归。
3. 再只加 TIM3 10 kHz PWM 和方向 GPIO，电机断开测波形。
4. 逐个接入 `firmware/` 的 host-tested 核心模块与薄 HAL 适配。
5. 每增加一个外设就按 `10_bringup_checklist.md` 留证据。
