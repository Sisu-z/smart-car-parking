# 智能车 Agent 工作规则

本仓库服务 STM32 + RDK X5 模块化机器人；用户要求中文协作、开源优先、控制成本。仓库公开不代表代码已实测或具备安全认证。

## 必读与真源

1. START_HERE.md → PROJECT_CONTEXT.md → NEXT_ACTIONS.md。
2. 任务涉及路线读 00_project_master_plan.md 与 DECISIONS.md；涉及代码读 INTERFACE_CONTRACTS.md、CONVENTIONS.md、MODULE_OWNERSHIP.md；读 HANDOFF.md 最新一节。
3. GitHub main 是已集成基线；分支/PR 是在途工作。事实以 01_hardware_source_of_truth.md 为准，当前开放问题以 OPEN_QUESTIONS.md 为准。
4. 旧聊天/历史交接不能覆盖最新用户要求和现行决策。发现冲突先指出并修正文档，不维护第二套“最终版/最新版”。

## 不可擅改的目标与安全边界

- 第一阶段必须普通车位线、随机位置/朝向自主找位、末段倒车入库、独立出库、跨场地验收；运行时禁止 AprilTag/二维码定位、人工起点/车位坐标、固定定时动作回放。M4 固定可见起点只是内部关卡。
- 允许棋盘离线标定；保留的 AprilTag 与模拟真值工具仅作实验，不能宣称主线已完成。
- 不冻结未经确认的底盘类型、相机数量、最终 PCB/CAD、完整 ROS2/SLAM、RTOS、机械臂或强化学习路线。采购须有实测理由并获授权。
- 厂家源资料与外部 test01 学习工程只读；未经明确集成授权不得覆盖。仓库不默认包含这些原件。
- 不把同系列电机参数、仿真常量或估计值写入实机配置。硬件审核门控默认关闭；烧录/运动/解除门控须有明确任务授权与安全证据。
- 停止指令不等于物理停稳，coast 不等于可靠刹车，软件超时不等于独立断能。未知空间不默认空闲。
- 硬件事实注明文件名+页/章节/代码位置；缺失标 UNKNOWN。项目库存口述标 PROJECT-CONTEXT。
- 严格区分 DESIGN / IMPLEMENTED / BUILD-VERIFIED / HOST-TESTED / SIMULATION-TESTED / TESTED / TESTED-BY-TEAM / UNVERIFIED / EXPERIMENTAL。编译或仿真不能升级成实车通过。
- 只读询问/诊断不自动授权修改、提交或推送；依当前任务授权行事。

## Git 协作：每次实际修改都维护

- 开始先检查分支、HEAD、工作树和远端。联网且可认证时 fetch；干净 main 只快进同步。脏工作树不强行 pull/rebase，不 reset/清理他人改动。
- 每个任务用独立分支，推荐 agent/<任务简名>；本机并行编辑用独立 worktree。在任务描述或 PR 记录基础提交、负责文件、依赖和完成标准，不共用同一工作目录写入。
- Specialist 只改任务边界内内容；接口/单位/枚举/频率/安全策略变更先提 DECISION 和契约草案，评审后才成为实施标准。不能由先写出代码者默认决定最终真相。
- 完成后先审查 diff、跑与风险相称的测试、检查公开内容，再明确列出文件提交；不得盲目 add 整个目录或夹带无关变化。
- 有写入/实现授权的任务按此维护约定提交、推送任务分支并提供提交/PR 链接；Integrator 审查后才能合入 main。无法联网/认证/推送须报告“仅本地，未同步”，不声称 GitHub 已更新。
- 不 force push、不重写共享历史、不删别人分支。集成时如已出现主线新提交，先检查冲突并重新验证。
- 一般文档整理可由 Integrator 自审；电源/电流、驱动安全、编码器比例、PI/PID、通信协议、泊车核心算法必须有独立 Reviewer 的证据审查。无审查者就保留候选/实验状态，不声称通过。
- 首次导入允许 Integrator 建立 main 基线，但保留代码实验/未上板/待独立审查状态；导入本身不是高风险代码审批。
- 本规则是每次任务的维护约定，不是后台自动任务，也不保证不同工具自行拉取远端。新 Agent 必须主动读取仓库并报告所用版本。

## 每次交付必须同步

- HANDOFF.md 最上方写当前阶段、Changes、Verified、Not Verified、Known Problems、Next Recommended Action，以及分支/基础提交或 PR。
- 只有状态变化时改 PROJECT_CONTEXT、NEXT_ACTIONS、OPEN_QUESTIONS；目标/接口/单位改变同步 DECISIONS 与相应真源。不改写历史测试为本轮新结果。
- 测试记录写命令、版本、范围、通过/失败/跳过、硬件是否参与。跳过不算通过，仿真拒绝不计正常泊车成功率。
- 交付报告包含改了什么、假设、验证/未验证、新问题和下一位 Agent 的动作。仓库链接不代替说明阻塞。

## 公开、许可与成本

- 不提交密钥/token、账号凭据、SSH配置、个人信息、原视频、学校/寝室隐私图像、厂家压缩包/PDF、外部 HAL、构建目录、虚拟环境、原始设备日志或本机配置。
- 原始来源可用相对逻辑路径+页码引用；未分发资料要注明不能仅凭公共仓库复核。不得复制未经许可的整包资料。
- 新开源依赖先核对来源/许可证/版本；保留归属，优先复用小而成熟的模块。PythonRobotics 是固定提交子模块，不默默升级到最新。
- 公开前人工审查暂存文件/差异；.gitignore 不是安全扫描或法律审查。项目自有部分未决定统一许可前，不擅自添加 MIT/Apache 覆盖他人内容。
- 尽量复用现有测试/回放/VOFA+，不堆框架。一般难题最多三次有依据的尝试，记录后继续独立工作；安全阻塞不能绕过。
